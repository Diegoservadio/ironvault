version: '3.8'

# ================================================================
# IronVault — Stack Monitoraggio
# DN Security Labs — ITS Academy Tech Talent Factory 2024-2026
# ================================================================
# Avvio: docker compose up -d
# Grafana:    http://192.168.143.140:3000  (admin/Cammellorosa2026!)
# Prometheus: http://192.168.143.140:9090
# ================================================================

services:

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./alert_rules.yml:/etc/prometheus/alert_rules.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    restart: unless-stopped
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.ignored-mount-points=^/(sys|proc|dev|host|etc)($$|/)'

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=Cammellorosa2026!
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SMTP_ENABLED=true
      - GF_SMTP_HOST=smtp.gmail.com:587
      - GF_SMTP_USER=marcoservadio03@gmail.com
      - GF_SMTP_PASSWORD=<APP_PASSWORD_GMAIL>
      - GF_SMTP_FROM_ADDRESS=marcoservadio03@gmail.com
      - GF_SMTP_FROM_NAME=IronVault Alert
      - GF_SMTP_STARTTLS_POLICY=MandatoryStartTLS

volumes:
  prometheus_data:
  grafana_data:
